# This configuration file should be installed on /etc/systemd/user/
# Only root is allowed to write to this directory
# After installation, the following commands need to be run from the user
# systemctl --user daemon-reload
# systemctl --user enable deej.service
# systemctl --user start deej.service
